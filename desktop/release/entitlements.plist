<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for Developer ID signing + notarization (direct download,
  NOT the Mac App Store, so no App Sandbox). CPython + PySide6/Qt need these exceptions:
  JIT / unsigned-executable memory for the interpreter, and disabled library validation so
  the app can load PyInstaller-bundled dylibs that aren't signed by the same Team ID.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
